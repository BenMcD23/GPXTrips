function toggleAccount(customerId, enable) {
//AJAX CODE NECESSARY TO MAKE THE CHANGE ON THE DB ... TO BE COMPLETED

    if (enable) {
        alert('Enabling account with ID ' + customerId);
    } else {
        alert('Disabling account with ID ' + customerId);
    }
}